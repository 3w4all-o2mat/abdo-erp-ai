name: Deploy to VPS

on:
  push:
    branches: [main, master]
  workflow_dispatch:

env:
  IMAGE_NAME: abdo-erp
  VPS_DEPLOY_DIR: /home/ubuntu/abdo-erp
  VPS_PORT: 3000

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: "true"
      ACTIONS_NODE_VERSION: "22"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: ${{ runner.os }}-buildx-

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          push: false
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache

      - name: Prepare SSH key
        if: always()
        run: |
          set -e
          mkdir -p ~/.ssh
          printf '%s' "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null || true
          echo "SSH key ready."

      # ---------------------------------------------------------------
      # The deploy script and docker-compose.yml are pulled directly
      # from the raw GitHub URL for the commit being deployed. This
      # removes any dependency on a git clone existing on the VPS or
      # on scp path-mapping quirks.
      # ---------------------------------------------------------------
      - name: Run deploy on VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 15m
          envs: NEW_IMAGE,NEW_TAG,PORT,COMMIT,DEPLOYED_AT
          script: |
            set -e
            DEPLOY_DIR="${{ env.VPS_DEPLOY_DIR }}"
            RAW_BASE="https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}"

            echo "=== Pre-flight ==="
            echo "DEPLOY_DIR: $DEPLOY_DIR"
            echo "RAW_BASE:   $RAW_BASE"
            if [ ! -d "$DEPLOY_DIR" ]; then
              mkdir -p "$DEPLOY_DIR"
              echo "       created (was missing)."
            fi
            cd "$DEPLOY_DIR"

            echo "=== Downloading deploy.sh and docker-compose.yml from GitHub ==="
            if command -v curl >/dev/null 2>&1; then
              curl -fsSL -o deploy.sh        "$RAW_BASE/scripts/deploy.sh"
              curl -fsSL -o docker-compose.yml "$RAW_BASE/docker-compose.yml"
            elif command -v wget >/dev/null 2>&1; then
              wget -q -O deploy.sh        "$RAW_BASE/scripts/deploy.sh"
              wget -q -O docker-compose.yml "$RAW_BASE/docker-compose.yml"
            else
              echo "ERROR: neither curl nor wget is available on the VPS" >&2
              exit 1
            fi
            chmod +x deploy.sh
            echo "       deploy.sh:        $(ls -la deploy.sh)"
            echo "       docker-compose.yml: $(ls -la docker-compose.yml)"

            echo "=== Running deploy.sh ==="
            NEW_IMAGE="${{ env.IMAGE_NAME }}:${{ github.sha }}" \
            NEW_TAG="${{ github.sha }}" \
            PORT="${{ env.VPS_PORT }}" \
            COMMIT="${{ github.sha }}" \
            DEPLOYED_AT="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
              bash "$DEPLOY_DIR/deploy.sh"

      - name: Cleanup SSH key
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
          echo "SSH key removed from runner."
